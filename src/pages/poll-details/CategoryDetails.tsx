import type {Category} from "../../models/types.ts";

interface Props {
    category: Category
}

const CategoryDetails = ({}: Props) => {
    //const {id, title, poll_id, created_at} = category;
    return <></>
}

export default CategoryDetails;